install.packages("rafalib")
install.packages("swirl")
library(swirl)
library(swirl)
version()
version()
# Create a numeric vector
my_vector <- c(2.23, 3.45, 1.87, 2.11, 7.33, 18.34, 19.23)
# Calculate the average
average_value <- mean(my_vector)
# Print the result
print(average_value)
# Initialize a variable to store the sum
sum_of_squares <- 0
# Use a for loop to calculate the sum of squares
for (i in 1:25) {
sum_of_squares <- sum_of_squares + i^2
}
# Print the result
print(sum_of_squares)
# Display the cars dataset
cars
# Use the class() function to determine the type of object
class_of_cars <- class(cars)
# Print the result
print(class_of_cars)
# View the first few rows of the cars dataset
head(cars)
# Use the class() function to determine the type of object
object_type <- class(cars)
# Print the result
print(object_type)
# Use the nrow() function to get the number of rows
num_rows <- nrow(cars)
# Print the result
print(num_rows)
# Use the names() function to get the column names
second_column_name <- names(cars)[2]
# Print the result
print(second_column_name)
The simplest way to extract the columns of a matrix or data.frame is using [. For example you can access the second column with cars[,2].
# Extract the second column (stopping distance)
stopping_distances <- cars[, 2]
# Calculate the average distance
average_distance <- mean(stopping_distances)
# Print the result
print(average_distance)
# Use which() to find the row where distance is 85
row_with_distance_85 <- which(cars$dist == 85)
# Print the result
print(row_with_distance_85)
x <- 1:10
y <- rnorm(10)
plot(x,y)
